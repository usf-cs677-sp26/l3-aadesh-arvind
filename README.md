# file-transfer

Since both Aravind and I didn't touch the proto file, the only difference was in our message handler, specifically where we send the retrieval response. He sent the checksum in that payload as well, where I didn't. Once we matched the message handler file, we were able to successfully transfer files. 
